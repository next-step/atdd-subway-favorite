spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  profiles:
    active:
      - test
    group:
      test:
        - test
      prod:
        - prod
    include:
      - env

security:
  jwt:
    token:
      secret-key: atdd-secret-key
      expire-length: 3600000


