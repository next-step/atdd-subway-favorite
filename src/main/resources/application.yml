spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password

  security:
    jwt:
      token:
        secret-key: anrndghkRhcdlvlduTtmqslek
        expire-length: 60000

logging:
  level:
    atdd: debug
