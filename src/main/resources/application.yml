spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false

security:
  jwt:
    token:
      secret-key: atdd-secret-key
      expire-length: 3600000
