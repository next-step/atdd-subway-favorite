spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

security:
  jwt:
    token:
      secret-key: atdd-secret-key
      expire-length: 3600000
